// Not silme işlemi için JavaScript kodu (henüz tam olarak yazılmadı)
function deleteProduct(productId) {
    console.log("productId: " + productId);
    // silme işlemi için gerekli kod buraya yazılacak
    // silme işlemi başarılı olursa sayfayı yenile
    location.reload();
}